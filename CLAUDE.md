## Critical Constraints

- Python 3.10/3.11 only. No backwards compatibility — remove old code cleanly.

### Running the Pipeline

- Config: `aii_pipeline/pipeline_config.yaml` — API keys in `.env` file.

```bash
aii_pipeline
```

