# AI Inventor — API Keys
# Copy this file to .env and fill in your actual API keys
#
# With default config (use_claude_agent: true):
#   - Claude CLI handles most work (authenticate via: claude login)
#   - GitHub CLI needed for repo creation (authenticate via: gh auth login)

# Required: image generation in gen_paper_repo (viz_gen via nano_banana skill)
GEMINI_API_KEY=

# Required: dataset downloads in invention_loop
HF_TOKEN=

# Required: web search in get_triples (use_aii_web_tools: true)
SERPER_API_KEY=

# Required if proof artifacts enabled (allowed_artifacts includes "proof")
LEANEXPLORE_API_KEY=

# Only if use_claude_agent: false (falls back to OpenRouter LLMs)
# OPENAI_API_KEY=
# OPENROUTER_API_KEY=
